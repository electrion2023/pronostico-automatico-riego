name: 🧪 Verificador Schedule

on:
  schedule:
    - cron: '*/5 * * * *'  # Cada 5 minutos
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: 📝 Verificar ejecución automática
        run: |
          echo "✅ VERIFICADOR EJECUTADO - $(date)"
          echo "Hora UTC: $(date -u)"
          echo "Hora Argentina: $(TZ='America/Argentina/Buenos_Aires' date)"
          echo "Timestamp: $(date +%s)"
